import React from "react";

// PUBLIC_INTERFACE
function RecipeResults() {
  /**
   * Displays search results as cards (stub)
   */
  return (
    <div>
      <div>Recipe results will appear here.</div>
    </div>
  );
}

export default RecipeResults;
