import React from "react";

// PUBLIC_INTERFACE
function FavoriteList() {
  /**
   * List container for saved recipes (stub)
   */
  return (
    <div>
      {/* List of favorite recipes (stub) */}
      <div>No favorites yet.</div>
    </div>
  );
}

export default FavoriteList;
